spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${keycloak.host}/realms/${keycloak.realm}
      client:
        registration:
          keycloak:
            client-id: ${keycloak.client.user-api.client-id}
            client-secret: ${keycloak.client.user-api.client-secret}
            authorization-grant-type: client_credentials
            scope: openid
        provider:
          keycloak:
            token-uri: ${keycloak.host}/realms/${keycloak.realm}/protocol/openid-connect/token

keycloak:
  host: ${KEYCLOAK_HOST}
  realm: ${KEYCLOAK_REALM}
  client:
    user-api:
      client-id: ${KEYCLOAK_CLIENT_USER_API_CLIENT_ID}
      client-secret: ${KEYCLOAK_CLIENT_USER_API_CLIENT_SECRET}